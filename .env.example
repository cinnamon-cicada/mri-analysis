STORAGE_BACKEND=firebase
QUEUE_BACKEND=firestore
FIREBASE_STORAGE_BUCKET=the-brain-benchmark-project.firebasestorage.app
GOOGLE_APPLICATION_CREDENTIALS=./backend/service-account-key.json
GCP_PROJECT=the-brain-benchmark-project
GCP_WORKER_JOB_LOCATION=us-central1
GCP_WORKER_JOB_NAME=mri-worker
FREESURFER_LICENSE=./license.txt
FASTSURFER_OUTPUT_DIR=./processed_data/web_jobs

# Firebase Auth (web client config, served by GET /api/firebase-config).
# These are public browser values, not secrets. Get them from:
#   firebase apps:sdkconfig WEB --project the-brain-benchmark-project
# If FIREBASE_API_KEY is unset, sign-in is disabled and /self shows a notice.
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=the-brain-benchmark-project.firebaseapp.com
FIREBASE_APP_ID=
