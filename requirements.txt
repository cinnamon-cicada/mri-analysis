# Web backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Data processing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
nibabel>=5.0.0

# Rate limiting
slowapi>=0.1.9

# Firebase / GCP (only needed when STORAGE_BACKEND=firebase or QUEUE_BACKEND=cloudtasks)
firebase-admin>=6.0.0
google-cloud-tasks>=2.13.0

# Testing
pytest>=7.4.0
httpx>=0.27.0
